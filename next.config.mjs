/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Export the application as static HTML for Netlify compatibility
  output: 'export',
};

export default nextConfig; 